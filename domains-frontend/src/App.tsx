import { BrowserRouter } from 'react-router';
import router from './router';

const App ${DB_USER:***REMOVED***} () ${DB_USER:***REMOVED***}> {
  return <BrowserRouter>{router()}</BrowserRouter>;
};

export default App;
