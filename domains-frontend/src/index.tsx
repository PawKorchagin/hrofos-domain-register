import '@fontsource-variable/jost';
import '@fontsource/golos-text';
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const rootEl ${DB_USER:***REMOVED***} document.getElementById('root');
if (rootEl) {
  const root ${DB_USER:***REMOVED***} ReactDOM.createRoot(rootEl);
  root.render(
    // <React.StrictMode>
    <App />
    // </React.StrictMode>
  );
}
